<!DOCTYPE html>
<html lang="en">
<head>
    <% if (info!=""){ %>
        <link rel="stylesheet" href="css/results_film.css" type="text/css">
        <link rel="stylesheet" href="css/stile.css" type="text/css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Risultati</title>
</head>
<body>
    <div class="my-navbar">
        <h3>
            MovieHub
        </h3>
        <div class="my-navbar-menù">
            <a href="/">Home</a>
            <a href="topMovie">Netflix Top 10</a>
            <%if (connected==true){%>
                <a href="profilo">Profilo</a>
                <form action='logout'>
                    <button>LOGOUT</button>
                </form> 
            <%}else{%>
                <div style="display:none">><a href="profilo">Profilo</a></div>
                <form action='login'>
                    <button>LOGIN</button>
                </form> 
            <%}%>
        </div>
    </div>
    <div class="main">
        <div class="main-box soft-shadow">
            <br>
            <h1>Ecco la Top 10 dei film più visti su Netflix</h1>
            <div class="film-box">
                <% if(info.length>0){ %>
                    <% for(var i=0;i < info.length; i++) {%>
                        <div class="film-external-card">
                            <a href="https://localhost:3000/results_topten?name=<%= info[i].name%>" style='text-decoration: none;'>
                                <button class="film-internal-card soft-shadow">
                                    <%= info[i].name%>
                                </button>
                            </a>
                        </div>
                    <%}%>
                <%}%>
            <%}%>
        </div>
    </div>
            
</body>
</html>    